package Util.symbols;

import Util.Type;
import Util.position;
import Util.scope.Scope;

public class ClassSymbol extends Symbol {
    public ClassSymbol(String _identifier, Scope _scope) {
        super(_identifier, _scope);
    }

}
